#include<bits/stdc++.h>
using namespace std;
using ListNode = std::forward_list<int>::iterator;
ListNode* reverseBetween(ListNode* head, int left, int right)
{
    
}
int main()
{
    list<int> myLinkedList;
    vector<int> arr = {1,2,3,4,5};
    for(auto it : arr)
        myLinkedList.push_back(it);
    forward_list<int> :: iterator head;;
    return 0;
}